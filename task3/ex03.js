// Exercise 3
// ------------------
// Get a list of planets that have "average" (i.e. (min + max) / 2) temperature within earthly bounds (between Earth's max and min)
const R = require("ramda");

const hospitablePlanets = () => {};

module.exports = hospitablePlanets;
